import React from "react";

const WeDeliverBestQualitySection = ({heading, paragraph}: any) => {
  return (
    <section className="we__deliver__best__quality__section">
      <h5>{heading}</h5>
      {paragraph}
    </section>
  );
};

export default WeDeliverBestQualitySection;
